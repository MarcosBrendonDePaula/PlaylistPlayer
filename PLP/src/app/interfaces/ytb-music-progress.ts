export interface YtbMusicProgress {
    percentage: Number,
    transferred: Number,
    length: Number,
    remaining: Number,
    eta: Number,
    runtime: Number,
    delta: Number,
    speed: Number,
    estimative: Number
}
